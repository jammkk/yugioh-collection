version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: yugioh
      POSTGRES_PASSWORD: yugioh
      POSTGRES_DB: collection
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
